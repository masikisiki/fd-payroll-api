de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.3.0

spring:
  mongodb:
    embedded:
      version: 4.8.2
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              ReactiveMongoTemplate: DEBUG
